#include <iostream>

using namespace std;

int main()
{
    int a,nums[],b;
   for(a=1; a<size; a++)
       for (b=size-1; b>=a; b--)
       {
           if (nums[b-1] > nums[b])
           {
               t= nums[b-1]
               nums[b-1] = nums [b]
               nums[b]=t;
           }
       }
}
